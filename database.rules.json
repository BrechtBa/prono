{
  "rules": {
    "pronogroupid1": {
      "wk2022": {
        ".read": "auth != null",
        ".write": "auth != null",
        "userpronos": {
          "$uid": {
            ".read": "auth != null && auth.uid == $uid",
            ".write": "auth != null && auth.uid == $uid"
          }
        }
      }
    }
  }
}